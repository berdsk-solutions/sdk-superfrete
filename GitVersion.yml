next-version: 0.1.0
mode: ContinuousDelivery
branches:
  master:
    tag: ""
    increment: Patch
    regex: ^master$
    is-release-branch: true
  develop:
    tag: preview
    increment: Minor
    regex: ^develop$
    source-branches: [ 'master' ]
    is-release-branch: false
ignore:
  sha: []