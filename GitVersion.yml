next-version: 1.2.1
mode: ContinuousDelivery
branches:
  master:
    tag: ""
    increment: Patch
    regex: ^master$
    is-release-branch: true
  develop:
    tag: preview
    increment: Minor
    regex: ^develop$
    source-branches: [ 'master' ]
    is-release-branch: false
ignore:
  sha: []